export interface operatorProps {
    operatorSymbol: string
    buttonColor: string
    width: number
    height: number
    marginTop?: string
}

export interface numberProps {
    buttonNumber: number
}